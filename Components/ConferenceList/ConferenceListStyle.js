import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    loadMoreButtonContainer: {
        margin: 10,
    },
    loadMoreButtonDivider: {
        marginBottom: 10,
    },
})