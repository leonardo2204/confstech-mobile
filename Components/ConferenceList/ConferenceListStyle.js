import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    innerContainer: {
        flex: 1,
    },
    title: {
        margin: 10,
        color: '#53acfe',
        fontSize: 16
    }
})